# Studio42 LCARS Edition v3.0.0 - Requirements
# Only install what's actually needed - graceful fallback for optional features

# ==================== CORE REQUIREMENTS (Always Needed) ====================
Pillow>=10.3.0
opencv-python>=4.9.0.80
numpy>=1.26.4

# ==================== CLASSIC STUDIO42 NODES ====================

# For rembg models (optional - graceful fallback)
onnxruntime-gpu>=1.18.0
rembg[gpu]>=2.0.57

# For BiRefNet models (optional - graceful fallback)
transformers>=4.30.0
timm

# For video processing (required for video features)
ffmpeg-python>=0.2.0

# For audio processing (optional - graceful fallback)
torchaudio>=2.0.0
soundfile>=0.12.1

# ==================== LCARS NODES ====================

# For Qwen AI models (optional - graceful fallback)
# transformers>=4.30.0  # Already listed above
torch>=2.0.0  # Usually included with ComfyUI

# For Wan video models (optional - graceful fallback)
diffusers>=0.25.0

# For procedural generation enhancements (optional)
scipy>=1.11.0

# ==================== FULL FEATURE INSTALLATION ====================
# To install everything at once:
#   pip install -r requirements.txt

# ==================== MINIMAL INSTALLATION ====================
# For just the base LCARS features (no AI models):
#   pip install Pillow opencv-python numpy

# ==================== NOTES ====================
# - PyTorch is usually included with ComfyUI
# - System FFmpeg installation recommended for optimal video/audio performance
# - All LCARS nodes have graceful fallbacks if dependencies are missing
# - Check LCARS_README.md for detailed installation instructions